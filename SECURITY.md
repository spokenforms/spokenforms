# Security

SpokenForms must not ingest real PHI, PCI, SSNs, credit card numbers, or real
call transcripts. Built-in sensitive entities are synthetic-only by default.

Report security concerns privately to the package maintainer.
